[project]
name = "mcp-servers-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.0.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
